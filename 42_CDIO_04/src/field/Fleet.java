package field;

import java.awt.Image;

public class Fleet extends Field{

	public Fleet(String desc, String subtext, String title, Image i, int pos) {
		super(desc,subtext,title,i,pos);
	}
}
